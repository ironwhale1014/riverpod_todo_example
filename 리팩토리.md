# 🚀 MoreOptionsButton 리팩토링 가이드

UI 로직 중복을 제거하고 코드 재사용성을 높이기 위해 `MoreOptionsButton` 위젯을 구현한 과정과 사용법을 안내합니다.

## 🎯 핵심 개념

### 1. 리팩토링 (Refactoring)
- **정의**: 코드의 겉보기 동작은 바꾸지 않으면서, 내부 구조를 개선하여 더 이해하기 쉽고, 수정하기 쉽게 만드는 과정입니다.
- **목표**: 코드 중복을 제거하고, 가독성을 높여 유지보수 비용을 줄이는 것입니다.
- **이번 사례**: `my_drawer.dart`와 `todo_card.dart`에 중복된 '더보기 메뉴' 코드를 하나의 공통 위젯으로 추출했습니다.

### 2. 관심사 분리 (Separation of Concerns, SoC)
- **정의**: 컴퓨터 프로그램을 각기 다른 역할을 하는 별개의 부분으로 나누는 설계 원칙입니다.
- **장점**: 각 부분이 독립적으로 개발, 테스트, 유지보수될 수 있어 전체 시스템의 복잡도가 낮아집니다.
- **이번 사례**: 복잡한 메뉴 생성 및 이벤트 처리 로직(관심사)을 `MoreOptionsButton`이라는 UI 컴포넌트로 분리하여, 기존 위젯은 데이터 표시에만 집중하도록 만들었습니다.

## ⚙️ 구현 단계

### 1단계: 문제 분석 및 목표 설정
- **문제점**: '수정/삭제' 팝업 메뉴를 띄우는 `IconButton` 관련 코드가 여러 파일(`my_drawer.dart`, `todo_card.dart`)에 그대로 중복되어 있었습니다.
- **목표**: 이 중복 코드를 하나의 재사용 가능한 위젯으로 만들어, 외부에서는 필요한 동작(콜백 함수)만 주입하여 간단히 사용할 수 있도록 개선합니다.

### 2단계: `MoreOptionsButton` 위젯 설계 및 구현
`lib/common/components/more_options_button.dart` 파일을 생성하고, 다음과 같이 위젯을 설계했습니다.

```dart
// lib/common/components/more_options_button.dart
import 'package:flutter/material.dart';

class MoreOptionsButton extends StatelessWidget {
  // 외부에서 수정/삭제 동작을 주입받기 위한 콜백 함수
  final VoidCallback? onEdit;
  final VoidCallback? onDelete;

  const MoreOptionsButton({super.key, this.onEdit, this.onDelete});

  @override
  Widget build(BuildContext context) {
    return Builder(
      builder: (context) {
        // 중복되던 모든 로직을 이 위젯 안으로 캡슐화
        return IconButton(
          onPressed: () async {
            // ... (RenderBox 위치 계산 및 showMenu 호출 로직)

            // 콜백이 null이 아닐 경우에만 메뉴 아이템을 동적으로 생성
            items: [
              if (onEdit != null)
                PopupMenuItem(value: 'edit', child: Text('수정하기')),
              if (onDelete != null)
                PopupMenuItem(value: 'delete', child: Text('삭제하기')),
            ],

            // ... (선택된 값에 따라 콜백 함수를 호출하는 switch문)
          },
          icon: Icon(Icons.more_vert),
        );
      },
    );
  }
}
```

**핵심 포인트**:
- `onEdit`, `onDelete` 콜백을 `nullable`로 선언하여, 수정 또는 삭제 기능 중 하나만 필요할 때도 유연하게 사용할 수 있도록 설계했습니다.
- `Builder` 위젯을 사용하여 `IconButton`의 `context`를 정확히 가져와 `RenderBox` 위치 계산에 사용합니다.

### 3단계: 기존 코드 리팩토링
기존의 복잡한 코드를 새로 만든 `MoreOptionsButton`으로 대체합니다.

> 💡 **팁**: 리팩토링 시, 기존 코드를 바로 삭제하지 말고 주석 처리한 뒤, 새 위젯이 동일하게 동작하는지 확인하고 나서 삭제하는 것이 안전합니다.

## 🔍 활용 예시

### 시나리오 1: `my_drawer.dart` 에서 카테고리 수정/삭제
```dart
// lib/screen/components/my_drawer.dart

// ...
rowData.addAll([
  MoreOptionsButton(
    onEdit: () {
      // 수정 다이얼로그를 띄우는 로직
      showDialog(
        context: context,
        builder: (context) => CategoryEditDialog(categoryWithCount.category!),
      );
    },
    onDelete: () {
      // 삭제 확인 다이얼로그를 띄우는 로직
      DialogUtils.showDeleteConfirmation(
        context,
        onConfirm: () => ref
            .read(categoryServiceProvider.notifier)
            .delete(categoryWithCount.category!),
      );
    },
  ),
]);
// ...
```
**설명**: `MoreOptionsButton`에 `onEdit`과 `onDelete` 동작을 각각 정의하여 전달합니다. 기존의 수십 줄에 달하던 코드가 단 몇 줄로 간결해졌습니다.

### 시나리오 2: `todo_card.dart` 에서 할 일 수정/삭제
```dart
// lib/screen/components/todo_card.dart

// ...
Row(
  children: [
    // ... (기존 UI)
    MoreOptionsButton(
      onEdit: () {
        showDialog(
          context: context,
          builder: (_) => EditDialog(todoEntry),
        );
      },
      onDelete: () {
        DialogUtils.showDeleteConfirmation(
          context,
          onConfirm: () => ref.read(todoServiceProvider.notifier).delete(todoEntry),
        );
      },
    ),
  ],
)
// ...
```
**설명**: `my_drawer.dart`와 동일한 `MoreOptionsButton` 위젯을 사용하지만, `onEdit`과 `onDelete`에 할 일(Todo)과 관련된 로직을 전달하여 재사용성을 극대화했습니다.

## 📋 완전한 구현 예시

```dart
// lib/common/components/more_options_button.dart
import 'package:flutter/material.dart';

class MoreOptionsButton extends StatelessWidget {
  const MoreOptionsButton({super.key, this.onEdit, this.onDelete});

  final VoidCallback? onEdit;
  final VoidCallback? onDelete;

  @override
  Widget build(BuildContext context) {
    return Builder(
      builder: (context) {
        return IconButton(
          onPressed: () async {
            final RenderBox renderBox = context.findRenderObject() as RenderBox;
            final offset = renderBox.localToGlobal(Offset.zero);

            final selectedValue = await showMenu(
              context: context,
              position: RelativeRect.fromLTRB(
                offset.dx + renderBox.size.width,
                offset.dy,
                offset.dx + renderBox.size.width,
                offset.dy,
              ),
              items: [
                if (onEdit != null)
                  PopupMenuItem(value: 'edit', child: Text('수정하기')),
                if (onDelete != null)
                  PopupMenuItem(value: 'delete', child: Text('삭제하기')),
              ],
              shape: RoundedRectangleBorder(
                borderRadius: BorderRadius.circular(8),
              ),
            );

            if (selectedValue != null) {
              switch (selectedValue) {
                case 'edit':
                  onEdit?.call();
                  break;
                case 'delete':
                  onDelete?.call();
                  break;
              }
            }
          },
          icon: Icon(Icons.more_vert),
        );
      },
    );
  }
}
```

## ⚠️ 주의사항 및 팁

1.  **`Builder` 위젯 사용**: `IconButton`을 `Builder`로 감싸야 `context.findRenderObject()`가 해당 `IconButton`의 `context`를 정확히 참조할 수 있습니다. `Builder` 없이 상위 `build` 메소드의 `context`를 사용하면 잘못된 위치를 계산할 수 있습니다.

2.  **유연한 메뉴 확장**: `MoreOptionsButton`에 `List<PopupMenuEntry<String>>? customItems` 와 같은 파라미터를 추가하면, '수정', '삭제' 외에 '공유', '복제' 등 다양한 메뉴 아이템을 외부에서 주입할 수 있도록 더 유연하게 확장할 수 있습니다.

3.  **상태 관리 라이브러리와 함께 사용 시**: `onConfirm` 콜백에서 `ref.read`를 사용하는 것은 괜찮지만, `ref.watch`를 사용하면 안 됩니다. 콜백은 상태 변화에 반응하여 재실행될 필요가 없기 때문입니다.

## 📝 핵심 원리 요약

> 복잡하고 중복되는 UI 로직을 **재사용 가능한 위젯으로 캡슐화**하고, 외부에서는 **콜백 함수를 주입**하는 방식으로 기능을 제어함으로써 코드의 **유지보수성과 가독성을 극대화**할 수 있습니다.