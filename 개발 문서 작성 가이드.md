
> 체계적이고 읽기 쉬운 기술 문서를 작성하기 위한 마크다운 템플릿 가이드입니다.

## 🎯 문서 구조 템플릿

### 기본 구조
```markdown
# [기술명] 구현/사용법 가이드

[기술에 대한 간단한 한 줄 설명]

## 핵심 개념

### 1. [개념명 1]
- 개념에 대한 설명
- 중요한 특징이나 장점
- 다른 방법과의 차이점

### 2. [개념명 2]
- 개념에 대한 설명
- 사용 시나리오

## 구현 단계

### 1단계: [단계명]
```[언어]
// 코드 예시
```

**설명**: 이 단계에서 하는 일에 대한 상세 설명

### 2단계: [단계명]
```[언어]
// 코드 예시
```

**중요 포인트**:
- 핵심 사항 1
- 핵심 사항 2

## 활용 예시

### [시나리오 1]
```[언어]
// 실제 사용 예시 코드
```

### [시나리오 2]
```[언어]
// 다른 사용 예시 코드
```

## 완전한 구현 예시

```[언어]
// 전체 코드 예시
```

## 주의사항

1. **[주의점 1]**: 설명
2. **[주의점 2]**: 설명

## 핵심 원리 요약

핵심 내용을 한두 문장으로 요약
```

---

## 📝 각 섹션 작성 가이드

### 1. 제목과 부제목
```markdown
# 📱 Flutter 상태관리 구현 가이드
특정 기술이나 문제 해결법에 대한 완전한 가이드

## 🎯 핵심 개념
## ⚙️ 구현 단계  
## 🔍 활용 예시
## ⚠️ 주의사항
## 📋 핵심 원리 요약
```

**이모지 사용 가이드**:
- 📱 모바일/앱 관련
- 🌐 웹 개발
- 🔧 도구/설정
- 📊 데이터/분석
- 🎨 UI/디자인
- 🔐 보안
- 🚀 성능/최적화

### 2. 핵심 개념 섹션
```markdown
## 핵심 개념

### 1. Provider 패턴
- Flutter에서 상태를 관리하는 디자인 패턴
- `ChangeNotifier`와 `Consumer`를 활용한 반응형 UI 구현
- `setState()`보다 효율적인 부분 업데이트 가능

### 2. 의존성 주입
- 상위 위젯에서 하위 위젯으로 상태 전달
- `Provider.of<T>(context)`를 통한 상태 접근
- 테스트 가능한 구조 제공
```

### 3. 구현 단계 섹션
```markdown
## 구현 단계

### 1단계: 의존성 추가
```yaml
dependencies:
  provider: ^6.0.5
```

**설명**: pubspec.yaml에 provider 패키지를 추가합니다.

### 2단계: 모델 클래스 생성
```dart
class Counter extends ChangeNotifier {
  int _count = 0;
  int get count => _count;
  
  void increment() {
    _count++;
    notifyListeners(); // UI 업데이트 알림
  }
}
```

**핵심 포인트**:
- `ChangeNotifier`를 상속받아 상태 변화를 알릴 수 있음
- `notifyListeners()`로 구독 중인 위젯들에게 업데이트 신호 전송
- private 변수와 getter를 사용해 캡슐화 구현
```

### 4. 코드 블록 작성법
````markdown
```dart
// 주석으로 코드 설명 추가
class MyWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Consumer<Counter>(
      builder: (context, counter, child) {
        return Text('${counter.count}'); // 상태값 표시
      },
    );
  }
}
```

**코드 설명**:
- `Consumer<Counter>`: Counter 상태 변화 감지
- `builder`: 상태 변화 시 호출되는 콜백 함수
- `counter.count`: 현재 카운터 값 접근
````

### 5. 시각적 설명 추가
```markdown
**Provider 동작 원리**:

```
MyApp (ChangeNotifierProvider)
├── Counter 상태 제공
│
├── HomePage
│   ├── Consumer<Counter> ← 상태 구독
│   └── Text('${counter.count}') ← 자동 업데이트
│
└── IncrementButton
    └── Provider.of<Counter>() ← 상태 조작
```

**상태 변화 흐름**:
1. 사용자가 버튼 클릭
2. `counter.increment()` 호출
3. `notifyListeners()` 실행
4. 구독 중인 모든 `Consumer` 위젯 재빌드
5. UI 자동 업데이트
```

### 6. 주의사항과 팁
```markdown
## 주의사항

1. **메모리 누수 방지**: 
   - `ChangeNotifier`는 `dispose()`에서 반드시 정리
   - `addListener()`를 사용했다면 `removeListener()` 호출 필요

2. **성능 최적화**:
   - `Consumer`보다 `Selector`를 사용해 필요한 부분만 업데이트
   - `context.read<T>()`와 `context.watch<T>()` 적절히 구분 사용

3. **타입 안전성**:
   - Generic 타입을 명시적으로 선언 (`Provider<CounterModel>`)
   - `Provider.of<T>(context, listen: false)` 옵션 활용
```

---

## 🚀 문서 작성 시 유용한 팁

### 1. 마크다운 확장 문법 활용
```markdown
> 💡 **팁**: 이렇게 하면 더 효율적입니다!

> ⚠️ **주의**: 이 부분에서 흔히 실수가 발생합니다.

> 🔍 **참고**: 추가 정보나 관련 링크

---

### 표를 활용한 비교
| 방법 | 장점 | 단점 | 사용 시기 |
|------|------|------|-----------|
| setState | 간단함 | 성능 이슈 | 작은 위젯 |
| Provider | 효율적 | 학습 곡선 | 복잡한 앱 |
| Riverpod | 타입 안전 | 최신 문법 | 새 프로젝트 |
```

### 2. 단계별 체크리스트
```markdown
## 구현 체크리스트

- [ ] 의존성 추가 (`pubspec.yaml`)
- [ ] 모델 클래스 작성 (`ChangeNotifier` 상속)
- [ ] Provider로 앱 래핑
- [ ] Consumer로 UI 구독
- [ ] 상태 업데이트 메서드 구현
- [ ] 메모리 정리 (`dispose()`)
```

### 3. 자주 사용하는 서식
```markdown
**중요한 내용**: 볼드체로 강조
*기울임체*: 용어나 변수명
`코드 조각`: 짧은 코드나 파일명
`fileName.dart`: 파일명
**`중요한 코드`**: 핵심 코드 강조

[링크 텍스트](URL): 외부 참조
[내부 링크](#섹션-제목): 문서 내 이동
```

---

## 💬 다른 Claude 채팅창에서 사용하는 방법

### 1. 템플릿 요청 방법
```
"[기술명]에 대한 개발 문서를 다음 구조로 작성해주세요:

1. 핵심 개념 (2-3개 주요 개념)
2. 단계별 구현 방법 (코드 예시 포함)
3. 실제 활용 예시 (2-3가지 시나리오)
4. 완전한 구현 예시
5. 주의사항과 팁
6. 핵심 원리 요약

각 코드 블록에는 상세한 주석과 설명을 포함해주시고, 
시각적 다이어그램이나 흐름도가 필요한 부분은 텍스트로 표현해주세요."
```

### 2. 구체적인 예시 프롬프트
```
"Flutter에서 Custom Paint를 사용한 차트 그리기에 대한 개발 문서를 작성해주세요. 

다음 형식을 따라주세요:
- 제목: # Flutter Custom Paint 차트 구현 가이드
- 핵심 개념: CustomPainter, Canvas, Paint 객체에 대한 설명
- 구현 단계: 1단계부터 차근차근 진행
- 예시: 바차트, 라인차트, 파이차트 구현
- 완전한 코드 예시 포함
- 성능 최적화 팁과 주의사항

각 코드에는 상세한 주석을 달고, 좌표 계산이나 수학적 개념이 들어가는 부분은 
텍스트 다이어그램으로 설명해주세요."
```

### 3. 문서 개선 요청 방법
```
"위 문서를 더 체계적으로 정리해주세요:

1. 섹션별로 이모지 추가
2. 코드 블록에 더 상세한 주석 추가  
3. 실제 개발에서 자주 마주치는 문제점과 해결책 추가
4. 성능 최적화 방법 섹션 추가
5. 관련 패키지나 라이브러리 추천 섹션 추가

마크다운 형식을 유지하면서 개발자가 바로 참고할 수 있는 
실용적인 레퍼런스 문서로 만들어주세요."
```

---

## 📋 문서 품질 체크리스트

작성한 문서가 다음 기준을 만족하는지 확인하세요:

### 구조와 가독성
- [ ] 명확한 제목과 부제목 구조
- [ ] 논리적인 정보 흐름 (개념 → 구현 → 예시)
- [ ] 적절한 이모지와 서식 사용
- [ ] 코드와 설명의 균형

### 내용의 완성도  
- [ ] 핵심 개념의 명확한 설명
- [ ] 단계별 구현 가이드
- [ ] 실제 사용 가능한 완전한 코드 예시
- [ ] 주의사항과 모범 사례 포함

### 실용성
- [ ] 복사해서 바로 사용 가능한 코드
- [ ] 흔한 실수와 해결 방법 언급
- [ ] 성능이나 보안 고려사항 포함
- [ ] 추가 학습 자료나 참고 링크

---

## 🎯 마무리

이 가이드를 활용하여 다른 Claude 채팅창에서도 일관되고 체계적인 개발 문서를 작성할 수 있습니다. 핵심은 **독자의 입장에서 필요한 정보를 논리적 순서로 제공**하는 것입니다.

**문서 작성 시 항상 기억할 점**:
- 복잡한 개념을 단순하게 설명
- 코드 예시는 실제 동작하는 완전한 형태로
- 독자가 따라 할 수 있는 명확한 단계 제시
- 실무에서 유용한 팁과 주의사항 포함