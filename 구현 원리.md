# 팝업 메뉴 위치 구현 원리

`Icons.more_vert` 버튼을 클릭했을 때, 버튼 바로 오른쪽에 수정/삭제 메뉴가 나타나도록 하는 기능의 구현 원리를 설명합니다.

## 핵심 개념

이 기능은 Flutter의 다음 두 가지 핵심 요소를 사용하여 구현됩니다.

1.  **`showMenu` 함수**: 특정 위젯 근처에 팝업 메뉴를 표시하는 데 사용되는 내장 함수입니다. 화면 하단에서 올라오는 `showModalBottomSheet`와 달리, `position` 파라미터를 통해 메뉴가 나타날 정확한 위치를 지정할 수 있습니다.

2.  **`RenderBox` 객체**: Flutter에서 위젯이 화면에 렌더링될 때, 해당 위젯의 실제 위치, 크기, 레이아웃 정보를 담고 있는 객체입니다. `RenderBox`를 사용하면 화면의 절대 좌표계에서 위젯이 어디에 있는지 정확히 알 수 있습니다.

## 구현 단계

메뉴를 버튼 바로 옆에 띄우기 위해 다음 단계를 거칩니다.

### 1. `showMenu` 함수 호출

먼저 `IconButton`의 `onPressed` 콜백 함수 내에서 `showMenu`를 호출합니다.

```dart
IconButton(
  onPressed: () {
    // ... 위치 계산 및 showMenu 호출 로직 ...
  },
  icon: Icon(Icons.more_vert),
)
```

### 2. 버튼의 `RenderBox` 가져오기

`context.findRenderObject()`를 호출하여 `IconButton`의 `RenderObject`를 가져온 후, 이를 `RenderBox` 타입으로 변환합니다. 이 `RenderBox`는 버튼의 크기와 위치 정보를 가지고 있습니다.

```dart
final RenderBox renderBox = context.findRenderObject() as RenderBox;
```

### 3. 버튼의 절대 좌표 계산하기

`renderBox.localToGlobal(Offset.zero)`를 사용하여 위젯의 로컬 좌표(왼쪽 상단 모서리, `Offset.zero`)를 화면 전체의 글로벌 좌표로 변환합니다. 이 결과로 나오는 `Offset` 객체는 화면 왼쪽 상단을 기준으로 한 버튼의 x, y 좌표를 담고 있습니다.

```dart
final offset = renderBox.localToGlobal(Offset.zero);
// offset.dx: 화면 왼쪽 끝에서 버튼 왼쪽 끝까지의 거리
// offset.dy: 화면 상단에서 버튼 상단까지의 거리
```

### 4. `showMenu`의 `position` 값 설정하기

계산된 좌표와 버튼의 크기를 사용하여 `RelativeRect` 객체를 생성하고, 이를 `showMenu`의 `position` 파라미터에 전달합니다. `RelativeRect`는 메뉴가 표시될 사각형 영역을 정의합니다.

```dart
showMenu(
  context: context,
  position: RelativeRect.fromLTRB(
    offset.dx + renderBox.size.width, // Left: 버튼의 오른쪽 끝
    offset.dy,                         // Top: 버튼의 상단
    offset.dx + renderBox.size.width,  // Right: (메뉴의 시작점이므로 Left와 동일하게 설정)
    offset.dy,                         // Bottom: (메뉴의 시작점이므로 Top과 동일하게 설정)
  ),
  items: [
    // ... PopupMenuItem 목록 ...
  ],
);
```

-   **`left`**: `offset.dx + renderBox.size.width`
    -   버튼의 x좌표(`offset.dx`)에 버튼의 너비(`renderBox.size.width`)를 더하여 **버튼의 바로 오른쪽**을 메뉴의 시작점으로 지정합니다.
-   **`top`**: `offset.dy`
    -   버튼의 y좌표(`offset.dy`)를 그대로 사용하여 **버튼과 같은 높이**에서 메뉴가 시작되도록 합니다.

## 요약

결론적으로, `RenderBox`를 통해 **버튼의 동적인 위치를 정확히 파악**하고, 이 좌표를 `showMenu` 함수의 `position` 값으로 전달하여 **메뉴가 나타날 위치를 버튼 바로 옆으로 정밀하게 제어**하는 것이 핵심 원리입니다.
