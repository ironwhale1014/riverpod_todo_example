# 🎨 동적 팝업 메뉴 위치 선정 구현 가이드

`IconButton` 클릭 시, 해당 버튼 바로 옆에 동적으로 팝업 메뉴를 띄우는 기능의 핵심 원리와 구현 방법을 설명합니다.

## 🎯 핵심 개념

### 1. `RenderBox`와 좌표계
- **`RenderBox`**: Flutter에서 위젯이 화면에 렌더링될 때 가지는 2D 박스 형태의 렌더링 객체입니다. 이 객체는 위젯의 **실제 크기(`size`)**와 **위치(`offset`)** 정보를 담고 있습니다.
- **좌표계**: Flutter에는 두 가지 주요 좌표계가 있습니다.
  - **로컬(Local) 좌표계**: 위젯 자체의 좌측 상단을 원점(0, 0)으로 하는 좌표계입니다.
  - **글로벌(Global) 좌표계**: 화면 전체의 좌측 상단을 원점(0, 0)으로 하는 좌표계입니다.
- **`localToGlobal()`**: `RenderBox`가 제공하는 메소드로, 로컬 좌표를 글로벌 좌표로 변환해주는 역할을 합니다. 이를 통해 특정 위젯이 화면 전체에서 어디에 위치하는지 정확히 알 수 있습니다.

### 2. `showMenu` 함수
- **정의**: 사용자의 상호작용에 대한 응답으로, 화면의 특정 위치에 메뉴를 표시하는 Flutter 내장 함수입니다.
- **`position` 파라미터**: `RelativeRect` 타입의 값을 받아, 메뉴가 나타날 위치를 지정합니다. 이 파라미터가 동적 위치 선정의 핵심입니다.

### 3. `RelativeRect`
- **정의**: 화면의 네 가장자리(Left, Top, Right, Bottom)로부터의 상대적 거리를 이용해 사각형 영역을 정의하는 객체입니다.
- **`fromLTRB()` 생성자**: `RelativeRect.fromLTRB(left, top, right, bottom)` 형태로 사용되며, 각 값은 화면 가장자리로부터의 픽셀 거리를 의미합니다.

## ⚙️ 구현 단계

### 1단계: 버튼의 `RenderBox` 객체 확보
사용자가 버튼을 누르는 시점(`onPressed`)에, `context`를 이용해 해당 위젯의 `RenderBox`를 가져옵니다.

```dart
// Builder 위젯의 context를 사용해야 정확한 RenderBox를 찾을 수 있음
final RenderBox renderBox = context.findRenderObject() as RenderBox;
```
> ⚠️ **주의**: `Builder` 위젯으로 `IconButton`을 감싸지 않으면, 상위 위젯의 `context`가 참조되어 잘못된 `RenderBox`를 가져올 수 있습니다.

### 2단계: 버튼의 글로벌 좌표 계산
`RenderBox`의 `localToGlobal` 메소드를 사용하여, 버튼의 로컬 원점(좌측 상단 모서리)이 화면 전체에서 어디에 위치하는지 계산합니다.

```dart
// 버튼의 좌측 상단(Offset.zero)을 글로벌 좌표로 변환
final offset = renderBox.localToGlobal(Offset.zero);

// offset.dx: 화면 왼쪽 끝에서 버튼 왼쪽 끝까지의 거리
// offset.dy: 화면 상단에서 버튼 상단까지의 거리
```

**좌표 변환 시각화**:

![좌표 변환 시각화](coordinate_visualization.svg)

### 3단계: `RelativeRect`로 메뉴 표시 위치 지정
계산된 글로벌 좌표와 버튼의 크기를 이용해 `RelativeRect`를 생성합니다. 이 때, **너비와 높이가 0인 점(Point)**을 만들어 메뉴의 기준점(Anchor Point)으로 삼는 것이 핵심입니다.

```dart
final position = RelativeRect.fromLTRB(
  // Left: 버튼의 x좌표 + 버튼의 너비 = 버튼의 오른쪽 끝
  offset.dx + renderBox.size.width,
  // Top: 버튼의 y좌표 = 버튼의 위쪽 끝
  offset.dy,
  // Right: Left와 동일한 값으로 설정하여 너비를 0으로 만듦
  offset.dx + renderBox.size.width,
  // Bottom: Top과 동일한 값으로 설정하여 높이를 0으로 만듦
  offset.dy,
);
```

**`RelativeRect` 동작 원리**:
- `left`와 `right` 값이 같으면, 사각형의 너비는 0이 됩니다.
- `top`과 `bottom` 값이 같으면, 사각형의 높이는 0이 됩니다.
- 결과적으로, 이 `RelativeRect`는 **버튼의 우측 상단 모서리**라는 정확한 한 점을 가리키게 됩니다.

### 4단계: `showMenu` 호출
계산된 `position` 값을 `showMenu` 함수에 전달하여, 해당 위치에 메뉴를 표시합니다.

```dart
await showMenu(
  context: context,
  position: position, // 3단계에서 계산한 바로 그 위치
  items: [ /* ... 메뉴 아이템들 ... */ ],
);
```

## 📋 완전한 구현 예시

이 모든 원리는 `MoreOptionsButton` 위젯에 다음과 같이 캡슐화되어 있습니다.

```dart
// lib/common/components/more_options_button.dart
import 'package:flutter/material.dart';

class MoreOptionsButton extends StatelessWidget {
  // ... (생성자)

  @override
  Widget build(BuildContext context) {
    return Builder(
      builder: (context) {
        return IconButton(
          onPressed: () async {
            // 1 & 2단계: RenderBox 확보 및 글로벌 좌표 계산
            final RenderBox renderBox = context.findRenderObject() as RenderBox;
            final offset = renderBox.localToGlobal(Offset.zero);

            // 3단계: RelativeRect로 메뉴 위치 지정
            final position = RelativeRect.fromLTRB(
              offset.dx + renderBox.size.width,
              offset.dy,
              offset.dx + renderBox.size.width,
              offset.dy,
            );

            // 4단계: showMenu 호출
            final selectedValue = await showMenu(
              context: context,
              position: position,
              items: [ /* ... */ ],
            );

            // ... (선택 값 처리 로직)
          },
          icon: Icon(Icons.more_vert),
        );
      },
    );
  }
}
```

## ⚠️ 주의사항 및 팁

1.  **정확한 `Context` 사용**: `RenderBox`를 찾을 때는 반드시 해당 위젯(`IconButton`)을 직접 감싸는 `Builder`의 `context`를 사용해야 합니다. 상위 위젯의 `context`를 사용하면 UI 트리가 변경될 때 잘못된 위치를 계산할 수 있습니다.

2.  **다른 위치에 메뉴 띄우기**: `RelativeRect`의 값을 조정하면 메뉴 위치를 자유롭게 변경할 수 있습니다. 예를 들어, 버튼의 **중앙 하단**에 메뉴를 띄우려면 다음과 같이 설정할 수 있습니다.
    ```dart
    RelativeRect.fromLTRB(
      offset.dx + renderBox.size.width / 2, // Left
      offset.dy + renderBox.size.height,    // Top
      offset.dx + renderBox.size.width / 2, // Right
      offset.dy + renderBox.size.height,    // Bottom
    );
    ```

## 📝 핵심 원리 요약

> `RenderBox`를 통해 위젯의 **글로벌 좌표**를 동적으로 계산하고, 이 좌표를 기준으로 너비와 높이가 0인 `RelativeRect` **기준점**을 만들어 `showMenu`에 전달함으로써, 어떤 위치에 있는 위젯이든 그 바로 옆에 정확하게 팝업 메뉴를 띄울 수 있습니다.